# URLs of your running JSON-RPC agents
AUTH_AGENT_URL=http://localhost:8100/a2a
WEBSERVICE_AGENT_URL=http://localhost:8101/a2a
DBSERVICE_AGENT_URL=http://localhost:8102/a2a

# The Gemini endpoint name (or full Vertex AI generateText endpoint)
VERTEX_MODEL=gemini-1.5-flash-002+

# --- Optional: Specify Vertex AI Project and Location if needed ---
# ADK often infers these from your gcloud default settings, but you can override them
VERTEX_PROJECT_ID=your-gcp-project-id
# VERTEX_LOCATION=us-central1 # Or your preferred region

# --- Optional: ADK Server Configuration ---
ADK_HOST=0.0.0.0 # Makes the ADK web UI accessible on your network
ADK_PORT=8000   # Default port
