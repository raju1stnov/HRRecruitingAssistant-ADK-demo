# HRRecruitingAssistant-ADK-demo/requirements.txt
fastapi
uvicorn[standard]
pydantic
httpx
google-adk # Use the correct ADK package
# google-api-python-client # Keep if needed for GCP auth, often included with ADK/Vertex
python-dotenv # Optional, for local .env file support